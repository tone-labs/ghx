# Binaries
/ghx
/dist/
*.exe

# Test / coverage artifacts
*.out
*.test

# Editor / OS
.DS_Store
.idea/
.vscode/

# Personal agent overlays. CLAUDE.local.md is symlinked to AGENTS.local.md so a
# single uncommitted file feeds both Claude Code and other agents.
AGENTS.local.md
CLAUDE.local.md

# Internal Linear routing (workspace/team). Kept local so the Linear CLI works;
# untracked so it doesn't ship in the public repo.
.linear.toml
